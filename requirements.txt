fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
websockets==16.1.1
# Drives a headless browser for the shipfinder scrape (server/shipfinder.py).
playwright==1.62.0
# Reads the marina webcam still. Headless: the container has no display and the
# GUI build drags in a stack of X libraries it would never use.
opencv-python-headless==4.12.0.88
numpy==2.2.6
# Reads the Sheriff's daily activity report, which is a PDF with a text layer
# (server/blotter.py). No OCR: the text is in there.
pypdf==6.10.2
